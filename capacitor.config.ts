import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.metercalculator.app',
  appName: 'MeterCalculator',
  webDir: 'dist'
};

export default config;
